apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-cert
  namespace: monitoring
spec:
  secretName: grafana-cert
  issuerRef:
    name: letsencrypt-aefeup
    kind: ClusterIssuer
  commonName: grafana.niaefeup.pt
  dnsNames:
    - grafana.niaefeup.pt
