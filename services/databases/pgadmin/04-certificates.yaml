apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pgadmin-cert
  namespace: pgadmin
spec:
  secretName: pgadmin-cert
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: pgadmin.niaefeup.pt
  dnsNames:
    - pgadmin.niaefeup.pt
