apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: oidc-auth-cert
  namespace: sso
spec:
  secretName: oidc-auth-tls
  dnsNames:
  - hydra.niaefeup.pt
  issuerRef:
    name: letsencrypt-production # Ensure this ClusterIssuer exists
    kind: ClusterIssuer