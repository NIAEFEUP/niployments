apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-cert
  namespace: test
spec:
  secretName: test-tls
  dnsNames:
    - test.niployments.local
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
