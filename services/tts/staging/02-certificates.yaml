apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tts-staging-certificate
  namespace: tts-staging
spec:
  secretName: tts-staging-certificate-secret
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - tts-staging.niaefeup.pt
