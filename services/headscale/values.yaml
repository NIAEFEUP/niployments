config:
  server_url: https://headscale.niaefeup.pt
  metrics_listen_addr: 0.0.0.0:9090

ingress:
  main:
    enabled: false

persistence:
  config:
    enabled: true
    storageClass: "longhorn-locality-retain"
    accessMode: ReadWriteOnce
    size: 2Gi
