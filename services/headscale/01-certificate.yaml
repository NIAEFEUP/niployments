---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: headscale-tls
  namespace: headscale
spec:
  secretName: headscale-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: headscale.niaefeup.pt
  dnsNames:
    - headscale.niaefeup.pt
