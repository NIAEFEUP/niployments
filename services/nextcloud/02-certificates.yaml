apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nextcloud-cert
  namespace: nextcloud
spec:
  secretName: nextcloud-cert
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: drive.niaefeup.pt
  dnsNames:
    - drive.niaefeup.pt